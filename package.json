{
  "name": "migrations",
  "scripts": {
    "start": "npm run synchronize && npm run seed",
    "synchronize": "ts-node src/synchronize.ts",
    "seed": "ts-node src/seed.ts"
  },
  "dependencies": {
    "mysql2": "^2.2.5",
    "reflect-metadata": "^0.1.10",
    "typeorm": "^0.2.30"
  },
  "devDependencies": {
    "@types/node": "^14.14.25",
    "prettier": "^2.2.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  }
}
